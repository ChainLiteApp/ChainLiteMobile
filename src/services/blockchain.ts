// This file now re-exports the legacy public API from the new modular compat layer.
// Keeping the filename ensures all existing imports continue to work without changes.
export * from './api/compat';
